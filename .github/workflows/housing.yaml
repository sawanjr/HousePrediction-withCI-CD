name: california-housing-price

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
    
      run: 
          echo Hello, world!
          pip install -r requiremnets.txt
          python housing_script.run
      
